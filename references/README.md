# Local reference inputs

Reference images are ignored by Git. Put an authorised local copy at the path named by the
movement configuration, for example:

```text
references/two_hand_snatches_pull_in.png
```

Before visual acceptance, verify that the file's dimensions and SHA-256 match the values in the
configuration. Do not commit third-party training photographs unless redistribution rights are
documented.
