{
  "name": "braven-studio",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5391 --strictPort",
    "build": "vite build",
    "test": "node --test tests/workspace.test.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 5397 --strictPort"
  },
  "dependencies": {
    "three": "0.169.0"
  },
  "devDependencies": {
    "vite": "8.3.0"
  }
}
